# 修正内容の確認（2026年4月21日）

提供された最新の画像情報に基づき、市場データと入札予定の更新を完了しました。

## 実施内容

### 1. 入札予定の更新（枕崎港：岬洋丸）
- `data/bid_schedule.json` に **2026-04-22** の **岬洋丸** (670t) のデータを追加しました。
- 画像から読み取ったサイズ別数量、海域情報、入札/相対の区分を正確に反映しました。
- 旧データの `is_latest` フラグを解除し、今回のデータを最新に設定しました。

### 2. 市場データの同期
- **焼津相場（4/16）** のデータが `market_input.csv` に正しく反映されていることを確認しました。
- `scripts/katsuo_fetcher.py` を実行し、CSVの最新状態を `katsuo_market_data.json` に同期しました。

### 3. バージョン情報の更新
- ブラウザのキャッシュ対策のため、`web/index.html` の `VERSION` を `20260421-1136` に更新しました。

## 確認事項
- `data/bid_schedule.json` の先頭に岬洋丸のデータが正しく追加されていることを確認済みです。
- トン数の合計（670t）が画像と一致していることを計算により確認済みです。

## 今後の作業
- 明日（4/22）の入札結果が判明次第、`market_input.csv` への登録が必要となります。
